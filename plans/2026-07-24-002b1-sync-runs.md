---
id: 002b1
title: Sync docs/domain/runs.md against actual codebase
status: ready
created: 2026-07-24
domain: docs/domain/runs.md
task: 002b1
---

# Plan: Sync docs/domain/runs.md

## Task and Domain Links

- **Task:** [002b1](../tasks/002b1-sync-domain-docs-runs.md)
- **Domain Doc:** `docs/domain/runs.md`

## Current State

`docs/domain/runs.md` was last updated 2026-07-16. It covers RunRecord, RunCheckpoint, baton sequence, execution flow, reconciliation reports, and lineage evidence.

## Objective

Read `docs/domain/runs.md` section by section, cross-reference against `db/models.py` (RunRecord, RunCheckpoint, ReconciliationReport, ReconciliationLineageRow) and `harness/conductor.py`.

## Out of Scope

- Any other domain doc files
- Backend implementation changes

## Blast Radius

One file: `docs/domain/runs.md`

## File Changes

- `docs/domain/runs.md` — patch in-place

## Verification

1. `grep -A 30 "class RunRecord" engine/src/migrations_engine/db/models.py` — verify all fields including `lookup_snapshot_versions` (JSON map vs singular)
2. `grep -A 20 "class RunCheckpoint" engine/src/migrations_engine/db/models.py` — verify
3. `grep -A 15 "class ReconciliationReport" engine/src/migrations_engine/db/models.py` — verify
4. `grep -A 15 "class ReconciliationLineageRow" engine/src/migrations_engine/db/models.py` — verify
5. Verify baton sequence in `harness/conductor.py`

## Commit

```
docs(domain): sync runs.md against codebase

Cross-reference docs/domain/runs.md against ORM models (RunRecord,
RunCheckpoint, ReconciliationReport, ReconciliationLineageRow) and
conductor. Fix field descriptions, baton sequence, and execution flow.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
```
