---
id: 002b0
title: Sync docs/domain/project.md against actual codebase
status: ready
created: 2026-07-24
domain: docs/domain/project.md
task: 002b0
---

# Plan: Sync docs/domain/project.md

## Task and Domain Links

- **Task:** [002b0](../tasks/002b0-sync-domain-docs-project.md)
- **Domain Doc:** `docs/domain/project.md`

## Current State

`docs/domain/project.md` was last updated 2026-07-26. It covers ProjectDefinition, ProjectRegistry, MigrationProjectConfig, snapshot policy, and related API endpoints.

## Objective

Read `docs/domain/project.md` section by section, cross-reference against `db/models.py` (ProjectDefinition, ProjectRegistry) and `routes/projects.py`. Verify all fields, types, and API endpoints.

## Out of Scope

- Any other domain doc files
- Backend implementation changes

## Blast Radius

One file: `docs/domain/project.md`

## File Changes

- `docs/domain/project.md` — patch in-place

## Verification

1. `grep -A 30 "class ProjectDefinition" engine/src/migrations_engine/db/models.py` — verify all fields
2. `grep -A 25 "class ProjectRegistry" engine/src/migrations_engine/db/models.py` — verify all fields
3. Verify MigrationProjectConfig fields match actual `domain_config` usage
4. Verify API endpoints: `PATCH /projects/{id}/manager`, `POST /projects/{id}/copy`, `POST /projects/{id}/codegen-instructions/reset`, `GET /config/ai-model-defaults`

## Commit

```
docs(domain): sync project.md against codebase

Cross-reference docs/domain/project.md against ORM models (ProjectDefinition,
ProjectRegistry) and project routes. Fix field descriptions, MigrationProjectConfig
schema, snapshot policy, and API endpoint accuracy.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
```
