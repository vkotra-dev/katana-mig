---
id: 002b4
title: Sync docs/domain/ui.md against actual codebase
status: ready
created: 2026-07-24
domain: docs/domain/ui.md
task: 002b4
---

# Plan: Sync docs/domain/ui.md

## Task and Domain Links

- **Task:** [002b4](../tasks/002b4-sync-domain-docs-ui.md)
- **Domain Doc:** `docs/domain/ui.md`

## Current State

`docs/domain/ui.md` was last updated 2026-07-20. It covers all UI screens, routes, components, and workflows. Several new UI features have been added since then (stacked source values, unmapped counts, mapping hints, discard feed, replacement upload).

## Objective

Read `docs/domain/ui.md`, then read the actual UI pages and components to verify each section. Add missing features, update stale descriptions.

## Out of Scope

- Any other domain doc files
- Frontend implementation changes

## Blast Radius

One file: `docs/domain/ui.md`

## File Changes

- `docs/domain/ui.md` — patch in-place

## Verification

1. Verify each route mentioned exists:
   - `/projects/[id]/feeds/[feedId]/page.tsx` — feed detail page
   - `/projects/[id]/feeds/[feedId]/review/page.tsx` — review page
   - `/projects/[id]/codegen/page.tsx` — codegen page
   - `/projects/[id]/change-requests/[crId]/page.tsx` — CR page
   - `/projects/[id]/feeds/[feedId]/fibers/[fiberId]/page.tsx` — fiber detail
2. Verify each component mentioned exists:
   - `ReviewGrid.tsx` — review grid component
   - `LookupMappingTable.tsx` — lookup mapping table
   - `KnowledgeFreezePanel.tsx` — knowledge freeze panel
   - `AiLogViewer` — AI log viewer
   - `UnifiedCommentThread` — comment thread
3. Verify fiber lifecycle states match actual fiber statuses
4. Verify role capabilities match actual route guards

## Pitfalls

- UI is the most volatile part of the codebase — doc drifts fastest here (~30% stale after just 4 days)
- New features (stacked source values, mapping hints, discard feed) are in the UI but not the doc
- Fiber lifecycle states in the doc may not match the simplified statuses actually used
- Lookup mapping grid structure in the doc doesn't reflect the `destinationMappings` data model

## Tests

N/A — documentation-only change, no test suite applies.

## Commit

```
docs(domain): sync ui.md against codebase

Cross-reference docs/domain/ui.md against actual UI pages and components.
Add missing features (stacked source values, unmapped counts, mapping hints,
discard feed, replacement upload), update fiber lifecycle states, and
fix lookup mapping grid structure descriptions.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
```
