---
id: 002aa
title: Sync docs/domain/auth.md against actual codebase
status: ready
created: 2026-07-24
domain: docs/domain/auth.md
task: 002aa
---

# Plan: Sync docs/domain/auth.md

## Task and Domain Links

- **Task:** [002aa](../tasks/002aa-sync-domain-docs-auth.md)
- **Domain Doc:** `docs/domain/auth.md`

## Current State

`docs/domain/auth.md` was last updated 2026-07-16. The codebase has evolved since then with:
- `session_version` field added to User model
- AuthSession stored in `auth_sessions` table (not pure stateless JWT)
- Role model finalized to 5 roles

## Objective

Update `docs/domain/auth.md` to accurately reflect the current codebase. Read every section of the doc, cross-reference against `engine/src/migrations_engine/db/models.py` (User, AuthSession) and `engine/src/migrations_engine/routes/auth.py`, and patch any divergences.

## Out of Scope

- Updating any other domain doc
- Changing any code
- Adding tests
- Updating API endpoints that are not auth-related

## Blast Radius

One file: `docs/domain/auth.md`

## File Changes

- `docs/domain/auth.md` — patch in-place to match code

## Verification

1. Diff the User class fields between doc and `db/models.py` line by line
2. Diff the AuthSession fields between doc and `db/models.py`
3. Grep all endpoints in `routes/auth.py` and verify each is listed in doc
4. Verify role enum matches the 5 canonical roles
5. Verify bootstrap path described matches `engine/src/migrations_engine/routes/auth.py` bootstrap logic
6. Run `grep -c "session_version\|AuthSession\|auth_sessions" engine/src/migrations_engine/db/models.py` to confirm session persistence is noted

## Pitfalls

- The doc says "stateless JWT" — need to reconcile this with the actual `auth_sessions` table. Either the doc needs updating or the behavior needs clarification (JWT + optional session revocation).
- The `session_version` field on User is used for session invalidation but is not documented.
- The `status` field default is "declared", not "active".

## Commit

```
docs(domain): sync auth.md against codebase

Cross-reference docs/domain/auth.md against ORM models (User, AuthSession)
and auth routes. Fix field descriptions, session persistence model, role
scope, and endpoint accuracy.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
```
