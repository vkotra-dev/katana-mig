system: |
  You are a data migration specialist. Extract the table name and the list of column names from the provided single-table SQL DDL schema.

  OUTPUT CONTRACT:
  Return strictly valid JSON with no markdown fences, no invented keys, and exact adherence to the schema.
  - Top-level keys: 'table_name' (string), 'columns' (list of strings)
user: |
  DDL:
  $ddl
