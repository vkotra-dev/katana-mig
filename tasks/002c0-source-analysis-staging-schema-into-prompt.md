---
id: 002c0
title: Inject staging_schema into source analysis prompt for qualified DDL generation
status: pending
created: 2026-07-24
priority: medium
depends-on: []
domain: engine
task: tasks/002c0-source-analysis-staging-schema-into-prompt.md
plan: plans/2026-07-24-002c0-source-analysis-staging-schema.md
---

# Task 002c0 — Source Analysis: Inject staging_schema into prompt

## Context

The source analysis AI generates a `CREATE TABLE` DDL for the source schema based on CSV sample data. Currently, the prompt receives `target_db_engine` (for SQL type selection) but NOT `staging_schema` (for schema-qualified table names). The DDL uses bare table names (e.g. `CREATE TABLE my_table (...)`) instead of qualified names (e.g. `CREATE TABLE staging.my_table (...)`).

Both `target_db_engine` and `staging_schema` live in `project_definition.domain_config`. The code already extracts `target_db_engine` but does not extract or pass `staging_schema` to the prompt.

**Open question — table naming convention:** This codebase uses `stg_{destination_object_name}` for staging table names (documented in `docs/domain/project.md:112`). The plan's proposed DDL uses the raw inferred source table name (not the `stg_` convention). This is informational-only — shown to the operator via copy-to-clipboard, never auto-applied — so it's not a functional bug. But showing an example that looks inconsistent with what "Generate SQL Bundle" produces later could confuse operators. **Decision needed:** should the generated DDL adopt the `stg_` naming convention to match, or is a plain source name intentional here since this describes the source shape, not the staging table?

## Current State

- `engine/src/migrations_engine/ai/prompts/source_analysis.yaml` — prompt template with placeholders `$target_db_engine`, `$sample_text`, `$source_type_section`. No `$staging_schema` placeholder.
- `engine/src/migrations_engine/management/source_analysis.py` line 85 — reads `target_db` from `domain_config.get("target_db_engine", ...)` but does not read `staging_schema`.
- `engine/src/migrations_engine/ai/prompt.py` — `Prompt` class uses Python `string.Template` substitution. Any `$var` in the template is replaced with the corresponding kwarg from `prompt.set()`.
- `engine/src/migrations_engine/api/schemas.py` line 179 — `staging_schema: str | None = None` in `MigrationProjectConfig`.
- `engine/src/migrations_engine/db/models.py` line 55 — `domain_config: Mapped[dict[str, Any] | None]` in `ProjectDefinition`.
- The DDL generated by the AI is stored in `SourceSchemaArtifact.destination_ddl` and used downstream for code generation.

## Objective

1. Add `$staging_schema` placeholder to the source analysis prompt template.
2. Extract `staging_schema` from `domain_config` in `analyze_source_slice` and pass it to the prompt alongside `target_db_engine`.
3. When `staging_schema` is present, the AI generates schema-qualified DDL (e.g. `CREATE TABLE staging.my_table (...)`).
4. When `staging_schema` is absent (None), the AI generates bare table names — backwards compatible.
5. Both `target_db_engine` and `staging_schema` are passed together so the AI uses the correct SQL types AND schema-qualified table names.

## Out of Scope

- Per-feed staging schema — this task uses the project-level `domain_config.staging_schema`.
- Changing how `destination_schema` is handled — that's a separate concern.
- Any frontend changes to the source analysis UI.
- Resolving the `stg_` naming convention question — surfaced above as an open decision point.

## Domain Updates Required

- `docs/domain/source-model.md` — update `SourceSchemaArtifact.destination_ddl` section to note that DDL now includes schema-qualified table names when `staging_schema` is configured.

## Pitfalls

1. `staging_schema` can be `None` — the prompt template must handle this gracefully. Use a conditional or default to empty string so the AI doesn't generate broken SQL.
2. The `$` prefix in `string.Template` means any `$` in the YAML content must be escaped as `$$` if it's not a placeholder. Check for existing `$` usage in the template.
3. The prompt template has two sections (`system` and `user`). The staging schema context should go in the `system` section where the DDL instruction lives.
4. Existing analyzed artifacts (with bare table names) will still be in the database — the fix only affects new analyses.

## Tests

- Unit test: `staging_schema` present → prompt includes schema name, AI generates qualified DDL.
- Unit test: `staging_schema` absent → prompt does not include schema name, AI generates bare table DDL.
- Integration: end-to-end test that a source analysis with a staging schema produces DDL with the schema-qualified table name.

## Commit

```
feat(source-analysis): inject staging_schema into prompt for qualified DDL

- Add $staging_schema placeholder to source_analysis prompt template
- Extract staging_schema from domain_config in analyze_source_slice
- When present, AI generates CREATE TABLE schema.table_name
- When absent, DDL generation is unchanged (backwards compatible)
```
