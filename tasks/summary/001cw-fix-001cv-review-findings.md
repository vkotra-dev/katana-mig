# Summary: 001cw — Fix 001cv Code Review Findings

## Objective
Fix the 4 actionable findings from the 001cv code review related to `generateTransformationInstructionsTemplate`.

## Changes Made
- Added a `stagingSchema` internal guard in `generateTransformationInstructionsTemplate` (`schema = stagingSchema || "staging"`).
- Renamed the inner `snapshot` variable to `snap` in the `snapshots.find` callback to eliminate variable shadowing.
- Reverted the `destEntryId` encoding to return a bare string instead of a JSON-quoted string (for non-null IDs) because a bare identifier looks better as a SQL identifier lookup key to the LLM. Retained `"null"` for the null case.
- Updated the assertions in `web/app/projects/[id]/codegen/page.test.tsx` to expect the new structure generated by `generateTransformationInstructionsTemplate`.

## Results
- `page.test.tsx` passes successfully.
- Code generation UI will output proper template data with accurate staging schema and unquoted `destEntryId` strings.
- TypeScript compilation is unaffected by these fixes.
